---
- hosts: all
  
  tasks:
  - name: ensure directory exists
    file:
      path: /tmp/test_dir
      state: directory
  - name: ensure file exists
    command: echo "hello world" touch test_file3
    args:
      chdir: /tmp/test_dir 
    
...
