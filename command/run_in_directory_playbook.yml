---
- hosts: all
  
  tasks:
  - name: ensure directory exists
    file:
      path: /tmp/test_dir
      state: directory
  - name: ensure file exists
    command: touch test_file5
    args:
      chdir: /tmp/test_dir 
    
...
